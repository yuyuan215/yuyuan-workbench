# 芋圆工作台 · 部署 / 安装 / 同步 说明

## 一、部署（更新到线上）
1. 解压 `yuyuan-workbench-deploy.zip`，把里面**所有文件**（含 `assets/` 子目录）覆盖到 GitHub 仓库 `yuyuan215/yuyuan-workbench` 根目录。
2. 提交并推送（Commit changes）。
3. 打开 https://yuyuan215.github.io/yuyuan-workbench/ ，按 **Ctrl+F5**（Mac 为 Cmd+Shift+R）硬刷新，清掉旧 Service Worker 缓存。

> 部署永久免费：GitHub Pages 长期托管，只要仓库在，工作台就一直在。

## 二、像 App 一样「点开就用」
工作台是 PWA（渐进式网页应用），可安装到电脑和手机：

- **电脑（Chrome / Edge / 360 极速等）**：浏览器右上角菜单（⋯）→「安装芋圆工作台 / Install app」。桌面出现图标，点开即全屏使用、可离线。
- **安卓（Chrome）**：右上角 ⋮ →「安装应用 / 添加到主屏幕」。
- **苹果（Safari）**：底部「分享」按钮 →「添加到主屏幕」。

安装后图标就是芋圆插画，从桌面/主屏启动即可，无需每次开浏览器输网址。

## 三、电脑与手机数据同步
数据默认存在本机浏览器。要让两端一致，需在「设置中心 → 云端同步」开启同步。**推荐用 GitHub 加密方式（最永久、无需第三方账号）**：

### 方式 A：GitHub 仓库加密同步（推荐）
1. 打开 https://github.com/settings/tokens?type=beta 新建**细粒度 Token（Fine-grained）**：
   - 仅授权你的 `yuyuan-workbench` 仓库；
   - 勾选 **Contents: Read and write**；
   - 过期可选「不过期」或设长一点。
2. 在「设置中心 → 云端同步」选择 **GitHub 仓库（加密，最永久）**，填写：
   - 仓库所有者：`yuyuan215`（你的 GitHub 用户名）
   - 仓库名：`yuyuan-workbench`
   - 文件路径：`sync-data.json`
   - 分支：`main`
   - Token：上面的 PAT
   - 同步密码：自设一个（**电脑和手机要填一样的**，用于加密数据）
3. 点「保存配置」→「立即同步」。数据会以**加密密文**存进仓库，即使仓库是公开的，别人也看不到内容。

### 方式 B：JSONBin.io（零后端，更简单）
1. 打开 https://jsonbin.io 免费注册，新建一个 Bin，复制 **Bin ID** 和 **Master Key (X-Master-Key)**。
2. 在「云端同步」选 JSONBin.io，填入 Bin ID 与 Key，保存并同步。

### 跨设备一键配置（同步码）
在电脑上配好云端后，点「**生成同步码**」（会自动复制一串码）；到手机上打开同一页面，点「**应用同步码**」粘贴，配置即自动配好，**不用手抄密钥**，两端立刻同步。

> 说明：同步按「记录时间戳合并」，多设备可同时使用；删除以墓碑方式传播，不会被旧数据复活。

## 四、安全提示
- GitHub 加密同步会把密文写入你的仓库；仓库公开也看不到明文（靠「同步密码」保护），请妥善保管该密码。
- 细粒度 PAT 仅授权这一个仓库，泄露影响面小；如担心，可在 GitHub 随时撤销并重发。
- 同步码包含云端密钥与密码，仅在你自己设备间传递，不要发给他人。

## 五、数据备份
「设置中心 → 数据管理」可一键**导出全部数据**（JSON），或导入/合并，作为本地备份。
